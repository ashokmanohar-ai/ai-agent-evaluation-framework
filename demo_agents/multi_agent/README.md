# Multi-agent demo

The deterministic adapter models `Supervisor → PolicyAgent/SupportAgent → Supervisor`
handoffs and records routing steps, source/destination, and payload. Dataset cases
exercise routing correctness, state preservation, and maximum-handoff loop gates.
