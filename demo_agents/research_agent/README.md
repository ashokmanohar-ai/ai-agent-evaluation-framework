# Research-agent adapter example

Use `GenericAgentAdapter` or `LangGraphAdapter` to connect a real research agent. The
framework consumes only the normalized `AgentTrace`; it does not depend on the
agent's orchestration library.
