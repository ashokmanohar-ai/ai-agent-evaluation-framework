# Dataset manifest

Version `1.0` (2026-08-21) contains 70 cases: 16 support, 16 tool-use, 12 recovery,
13 safety and 13 multi-agent/memory cases. IDs are immutable within a version.

Changes require a reviewed changelog entry describing case additions, removals,
expectation changes and baseline impact. Do not add real customer data, secrets or
executable commands. Validate all referenced tools against `config/tool-policy.yaml`.
